# Core
Django==5.2.1
djangorestframework==3.16.0
psycopg2-binary==2.9.12

# Auth & Keycloak
python-keycloak==5.5.0
PyJWT==2.10.1

# API
drf-spectacular==0.28.0
django-filter==25.1
django-cors-headers==4.7.0
django-extensions==4.1

# Caching & Async
django-redis==5.4.0
redis==6.0.0
celery==5.5.2
django-celery-beat==2.8.0
django-celery-results==2.6.0

# Realtime (chat WebSockets)
channels==4.2.0
channels-redis==4.2.1
daphne==4.1.2

# Chat message storage
pymongo==4.17.0

# Malware scanning (chat attachments)
clamd==1.0.2

# Storage (MinIO / S3)
boto3==1.38.12
Pillow==11.2.1

# History / Audit
django-simple-history==3.8.0

# Static files
whitenoise==6.9.0

# Finance — PDF generation (optional; falls back to print-ready HTML if absent)
xhtml2pdf==0.2.16

# Utils
django-cleanup==9.0.0
python-decouple==3.8
loguru==0.7.3
requests==2.32.3
django-jazzmin==3.0.4
